name: Python Workflow

on: [push]

jobs:
  install-python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Download Python 3.11
        run: |
          wget https://www.python.org/ftp/python/3.11.0/Python-3.11.0.tgz
          tar -xzf Python-3.11.0.tgz
          cd Python-3.11.0
          ./configure --prefix=/home/runner/python3.11 --enable-optimizations
          make altinstall
          cd ..
          rm -rf Python-3.11.0 Python-3.11.0.tgz

      - name: Upload Python as an artifact
        uses: actions/upload-artifact@v3
        with:
          name: python-3.11
          path: /home/runner/python3.11/bin/python3.11

  use-python:
    runs-on: ubuntu-latest
    needs: install-python
    steps:
      - name: Download Python artifact
        uses: actions/download-artifact@v3
        with:
          name: python-3.11

      - name: Check Python version
        run: |
          /home/runner/python3.11/bin/python3.11 --version
