name: print

on: push

env:
  CONTINUE: true
  TIME: 3

jobs:
  job1:
    runs-on: ubuntu-latest
    # continue-on-error cannot use dynamic expressions or environment variables directly
    continue-on-error: true  # Set to a fixed boolean value if needed
    
    timeout-minutes: 3  # Direct value; could be adjusted as needed

    steps:
      - name: Check environment variable and handle error
        run: |
          echo "This is a step that might fail"
          # You can use environment variables within steps
        continue-on-error: ${{ env.CONTINUE == 'true' }}  # Within step

      - name: Another step
        run: echo "This step will always run"
