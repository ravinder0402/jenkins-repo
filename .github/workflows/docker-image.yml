name: Masking Secret Example

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  bash-example:
    runs-on: ubuntu-latest
    steps:
      - name: Mask secret
        run: |
             stopMaker=$(uuidgen)
             echo"$stopMaker"


       

