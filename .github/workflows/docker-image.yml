name: Masking Secret Example

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  bash-example:
    runs-on: ubuntu-latest
    steps:
      - name: Mask secret
        run: |
          echo "::add-mask::${{ vars.MY_NAME }}"
          echo "Masked value is set."


       

