name: Masking Secret Example

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  bash-example:
    runs-on: ubuntu-latest
    steps:
     - name: Set the value in bash
       id: step_one
       run: |
           {
            echo 'JSON_RESPONSE<<EOF'
            curl https://example.com
            echo EOF
           } >> "$GITHUB_ENV"

     - name: environment varialbe value
       run: echo "$JSON_RESPONSE"
     - name: Job summary
       run: echo "### Hello world! :rocket:" >> $GITHUB_STEP_SUMMARY



       

